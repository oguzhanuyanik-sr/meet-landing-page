import Logo from './common/Logo';

const Navbar = () => {
  return (
    <header>
      <nav className='flex justify-center mt-12'>
        <Logo />
      </nav>
    </header>
  );
};

export default Navbar;
